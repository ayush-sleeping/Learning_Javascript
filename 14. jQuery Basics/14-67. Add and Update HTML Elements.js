/* The "html()" Method :
The equivalent of JavaScript's innerHTML in jQuery is the html()
    method. We can use the html() method to get content of
    selected HTML element. This content can either be text or 
    more HTML elements.
    
Syntax :
To get values: $('selector').html()
To set values: $('selector).html(childElement)

*/